# Databricks notebook: entity_resolution_runner
import json

config_json = dbutils.widgets.get("config_json")
cfg = json.loads(config_json)

# Import pipeline from the app's repo (if using Databricks Repos)
# or just paste the pipeline functions inline here
exec(open("/path/to/pipeline.py").read())

run_pipeline(cfg, spark)
