SynapseBuiltinSqlPoolRequestsEnded
| where TimeGenerated > ago(24h)
| project 
    TimeGenerated, 
    Database = tostring(Properties.databaseName), 
    DataMB = todouble(Properties.dataProcessedBytes) / 1024 / 1024,
    User = Identity,
    QueryText = tostring(Properties.queryText)
| take 10
