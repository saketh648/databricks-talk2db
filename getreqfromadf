union 
(SynapseBuiltinSqlPoolRequestsEnded | project Name = tostring(Properties.databaseName)),
(SynapseSqlPoolSqlRequests | project Name = ResourceGroupName_s) // Or specific pool column
| summarize by Name
| where isnotempty(Name)
