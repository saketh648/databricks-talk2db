.withColumn("DataProcessedGB",
    F.round(F.col("Bytes") / (1024.0 * 1024.0 * 1024.0), 6)  # 6 decimal places instead of 3
)
.withColumn("EstimatedCostUSD",
    # Azure Serverless pricing is $5 per TB scanned = $0.004882 per GB
    F.round((F.col("Bytes") / (1024.0 * 1024.0 * 1024.0 * 1024.0)) * 5.0, 6)  # price per TB not GB
)
