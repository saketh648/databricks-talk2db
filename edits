# Check if the Synapse Log Analytics emitter is active
enabled = spark.conf.get("spark.synapse.logAnalytics.enabled", "false")
workspace_id = spark.conf.get("spark.synapse.logAnalytics.workspaceId", "Not Found")

print(f"Log Analytics Enabled: {enabled}")
print(f"Target Workspace ID:  {workspace_id}")
