import os

# Search for pipeline.py everywhere under your user folder
for root, dirs, files in os.walk("/Workspace/Users"):
    for file in files:
        if "pipeline" in file.lower():
            print(os.path.join(root, file))
