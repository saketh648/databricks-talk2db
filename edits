import json
import sys

# Point to your repo where pipeline.py lives
sys.path.insert(0, "/Workspace/Repos/your-email/your-repo-name")

from pipeline import run_pipeline

# Widget to receive config from the app
dbutils.widgets.text("config_json", "{}")
config_json = dbutils.widgets.get("config_json")
cfg = json.loads(config_json)

print("Config received:")
print(json.dumps(cfg, indent=2))
print("Starting pipeline...")

run_pipeline(cfg, spark, log_fn=print)

print("Pipeline complete!")
